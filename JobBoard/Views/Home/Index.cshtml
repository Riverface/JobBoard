@using JobBoard.Models;
<h1></h1>
<ul>
@if (Model.Count == 0)
{
  <p>There are no Parcels in the list.</p>
}
<ul>
  @foreach (JobOpening opening in Model)
  {
    <li><a href="">@opening.Name</a></li>
  }
</ul>

<a href="/parcel/new">Add a new Parcel.</a> 